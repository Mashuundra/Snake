"""
config.py
Конфигурационные параметры игры "Змейка"

Содержит все настройки: размеры, цвета, скорость.
Изменяя значения здесь, можно настраивать игру без правки кода.
"""

""" РАЗМЕРЫ ИГРОВОГО ПОЛЯ (в клетках) """
# Ширина поля по умолчанию
DEFAULT_WIDTH = 20

# Высота поля по умолчанию
DEFAULT_HEIGHT = 20

# Минимальные и максимальные размеры поля (для валидации)
MIN_WIDTH = 5
MAX_WIDTH = 50
MIN_HEIGHT = 5
MAX_HEIGHT = 50

""" НАСТРОЙКИ КАРТЫ """
# Тип карты по умолчанию:
# "empty" - пустая карта "obstacles" - карта с внутренними препятствиями
DEFAULT_MAP_TYPE = "empty"

# Цвет препятствий (для PyQt/Tkinter)
COLOR_OBSTACLE = (100, 100, 100)  # серый

""" ВИЗУАЛЬНЫЕ ПАРАМЕТРЫ """
# Размер одной клетки в пикселях
DEFAULT_CELL_SIZE = 30

# Минимальный и максимальный размер клетки
MIN_CELL_SIZE = 10
MAX_CELL_SIZE = 60

# Вычисляемые размеры окна (не изменять вручную)
# Эти значения рассчитываются автоматически из width * cell_size
# DEFAULT_WINDOW_WIDTH = DEFAULT_WIDTH * DEFAULT_CELL_SIZE
# DEFAULT_WINDOW_HEIGHT = DEFAULT_HEIGHT * DEFAULT_CELL_SIZE


""" СКОРОСТЬ ИГРЫ """
# Скорость по умолчанию (кадров в секунду)
DEFAULT_SPEED = 10

# Минимальная и максимальная скорость
MIN_SPEED = 3
MAX_SPEED = 30

""" ЦВЕТА (RGB формата) """
# Каждый цвет — это кортеж из трёх чисел от 0 до 255

# Цвет фона игрового поля (чёрный)
COLOR_BACKGROUND = (0, 0, 0)

# Цвет головы змейки (ярко-зелёный)
COLOR_SNAKE_HEAD = (0, 255, 0)

# Цвет тела змейки (тёмно-зелёный)
COLOR_SNAKE_BODY = (0, 200, 0)

# Цвет еды (красный)
COLOR_FOOD = (255, 0, 0)

# Цвет сетки (тёмно-серый, почти невидимый)
COLOR_GRID = (40, 40, 40)

# Цвет текста (белый)
COLOR_TEXT = (255, 255, 255)

# Цвет текста Game Over (красный)
COLOR_GAME_OVER = (255, 100, 100)

""" НАСТРОЙКИ ОТРИСОВКИ """
# Рисовать ли сетку на поле
DRAW_GRID = True

# Толщина линии сетки в пикселях
GRID_LINE_WIDTH = 1

# Скругление углов змейки и еды (0 = квадратные, больше = круглее)
SNAKE_CORNER_RADIUS = 0
FOOD_CORNER_RADIUS = 0

""" НАСТРОЙКИ ТЕКСТА """
# Размер шрифта для счёта
FONT_SIZE_SCORE = 24

# Размер шрифта для Game Over
FONT_SIZE_GAME_OVER = 48

# Размер шрифта для подсказок
FONT_SIZE_HINT = 16

# Отступ текста от края окна (в пикселях)
TEXT_MARGIN = 10

""" ПАРАМЕТРЫ ЗМЕЙКИ (логика) """
# Начальная позиция змейки (x, y) — центр поля
# Вычисляется как DEFAULT_WIDTH // 2, DEFAULT_HEIGHT // 2

# Начальное направление (1, 0) = вправо
# (-1, 0) — влево, (0, 1) — вниз, (0, -1) — вверх
INITIAL_DIRECTION = (1, 0)

""" НАСТРОЙКИ ТЕКСТОВОЙ ОТРИСОВКИ (для терминала) """
# Символы для отрисовки в терминале
# Можно изменить на любые другие символы по вашему вкусу

# Голова змейки
SYMBOL_SNAKE_HEAD = '●'

# Тело змейки
SYMBOL_SNAKE_BODY = '○'

# Еда
SYMBOL_FOOD = '★'

# Пустая клетка
SYMBOL_EMPTY = '·'

# Стена
SYMBOL_WALL = '#'

# Рисовать ли стены по краям поля
# True - стены есть (змейка не может выйти за границы поля)
# False - стены отсутствуют (змейка может выходить за границы, но это вызовет Game Over)
DRAW_WALLS = True

# Альтернативные символы для простых терминалов (если символы юникода не отображаются)
# Раскомментируйте следующие строки, если ваш терминал не поддерживает юникод

# SYMBOL_SNAKE_HEAD = '@'
# SYMBOL_SNAKE_BODY = 'o'
# SYMBOL_FOOD = 'F'
# SYMBOL_EMPTY = '.'
# SYMBOL_WALL = '#'

# Включить ли поддержку стрелок наряду с WASD
# True - поддерживаются и стрелки, и WASD
# False - только WASD (для совместимости)
ENABLE_ARROW_KEYS = True

# Показывать ли подсказки по управлению во время игры
SHOW_CONTROLS_HINT = True

# Использовать ли цвет в терминале (если поддерживается)
# True - использовать ANSI-цвета
# False - чёрно-белый режим
USE_TERMINAL_COLORS = False

# Настройки цветов для терминала (ANSI escape-коды)
# Работает только если USE_TERMINAL_COLORS = True
TERMINAL_COLOR_SNAKE_HEAD = '\033[92m'  # Ярко-зелёный
TERMINAL_COLOR_SNAKE_BODY = '\033[32m'  # Тёмно-зелёный
TERMINAL_COLOR_FOOD = '\033[91m'       # Красный
TERMINAL_COLOR_WALL = '\033[90m'       # Тёмно-серый
TERMINAL_COLOR_RESET = '\033[0m'       # Сброс цвета